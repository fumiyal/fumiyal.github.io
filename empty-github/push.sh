

# cd "$(dirname "$0")"

git add .
git commit -m "Initial; Created the app."
git branch -M main
git remote add origin https://github.com/KusoGames/Kuso-Physics.git
git push --force -u origin main

# cd -

